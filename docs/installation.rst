Installation
============

Prerequisites
-------------
* Python 3.8+
* pip

Install from source
-------------------
.. code-block:: bash

   git clone <your-repository-url>
   cd pseudodynamics
   pip install -e .

Dependencies
------------
All dependencies are listed in ``setup.py`` and will be installed automatically.